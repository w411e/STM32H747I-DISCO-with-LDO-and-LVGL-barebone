{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "STM32H747_D",
            "cwd": "${workspaceFolder}",
            "executable": "${workspaceFolder}/.build/h747disco_tut.elf",
            "preLaunchTask": "Flash (CubeProgrammer)",
            "request": "launch",
            "type": "cortex-debug",
            "servertype": "stlink",
            "device": "STM32H747XI",
            "interface": "swd",
            // "preLaunchCommands": [
            //     "monitor reset",
            //     "monitor halt"
            // ],
            "runToEntryPoint": "main"
        }
    ]
}